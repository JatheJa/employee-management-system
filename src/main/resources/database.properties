# Employee Management System Database Configuration
# Update these settings to match your MySQL database setup

# Database connection details
db.url=jdbc:mysql://localhost:3306/employee_management_system?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/New_York
db.username=root
db.password=Kevinperdomo1
db.driver=com.mysql.cj.jdbc.Driver

# Connection pool settings (optional - defaults will be used if not specified)
db.pool.minimumIdle=5
db.pool.maximumPoolSize=20
db.pool.connectionTimeout=30000
db.pool.idleTimeout=600000

# Database settings
db.characterEncoding=utf8
db.useUnicode=true

# Performance settings
db.cachePrepStmts=true
db.prepStmtCacheSize=250
db.prepStmtCacheSqlLimit=2048
db.useServerPrepStmts=true

# Notes:
# - Make sure MySQL server is running before starting the application
# - Create the database 'employee_management_system' using the provided schema.sql
# - Update username and password to match your MySQL configuration
# - For production, consider using environment variables for sensitive data